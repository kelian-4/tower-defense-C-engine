/*
 * pathfinder.c — DÉSACTIVÉ
 * La génération procédurale de chemin a été retirée.
 * Ce fichier est conservé comme stub vide pour la compilation.
 */
#include "pathfinder.h"

PathPoint current_path[500];
int current_path_len = 0;

/* Stub vide — ne génère rien. */
void generer_chemin(int niveauID) {
    (void)niveauID;
    current_path_len = 0;
}
